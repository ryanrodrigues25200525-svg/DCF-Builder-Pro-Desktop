SEC_SERVICE_URL=https://your-render-service.onrender.com
NEXT_PUBLIC_SEC_SERVICE_URL=https://your-render-service.onrender.com

# Optional SEC header identity for server-side SEC requests only
SEC_USER_AGENT=DCFBuilder_Research/1.0 (security@example.com)

# Production note:
# Use the Cloud Run backend URL for both values.
# Keep real secrets and deployment values in Vercel project settings only.
